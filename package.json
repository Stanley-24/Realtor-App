{
  "name": "realtor-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npm install --include=dev --prefix server && npm install --include=dev --prefix client && npm run build --prefix client && npm run build --prefix server",
    "start": "npm run start --prefix server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
